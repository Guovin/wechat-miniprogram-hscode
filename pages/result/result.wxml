<!--result.wxml-->
<view class="result">
  <include src="../../components/default/default.wxml"></include>
  <search></search>
  <!-- toptips请求提示 -->
  <mp-toptips msg="{{msg}}" type="{{type}}" show="{{show}}"></mp-toptips>
  <view class="search_result">
  <view wx:for="{{dataList}}" wx:key="*this">{{index+1}}:{{item.hscode_name}}</view>
  </view>
</view>